<div data-hook="products_search_results_heading">
  <% if taxons.empty? %>
    <div data-hook="products_search_results_heading_no_results_found">
      <%= t('spree.no_products_found') %>
    </div>
  <% elsif params.key?(:keywords) %>
    <div data-hook="products_search_results_heading_results_found">
      <h6 class="search-results-title"><%= t('spree.search_results', keywords: h(params[:keywords])) %></h6>
    </div>
  <% end %>
</div>

<% if taxons.any? %>
  <% taxons.each_with_index do |taxon, i| %>
    <% if i % 8 == 0 %>
      <% reset_cycle("classes") %>  
    <% end %>  
    <%= render partial: 'spree/products/glass_photo_prints_product', locals: { taxon: taxon } %>
  <% end %>    
<% end %>
