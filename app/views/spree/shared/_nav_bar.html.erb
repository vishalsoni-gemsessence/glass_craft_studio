<!-- <nav id="top-nav-bar" class="columns ten"> -->
  <!-- <ul id="nav-bar" class="navbar-nav ml-auto js-collapse-activeitem" data-hook> -->
    <%#= render 'spree/shared/login_bar_items' %>
    <!-- <li id="search-bar" data-hook> -->
      <%= render partial: 'spree/shared/search' %>
    <!-- </li> -->

    <!--/ Visible 1200 and more -->
    <%= render partial: 'spree/shared/locale_selector' %>

    <!--/ Visible 1199 and min-->
    <% available_locales = current_store.available_locales %>
    <% if available_locales.many? %>
      <%= form_tag spree.select_locale_path do %>
        <li class="nav-item mb-5 d-xl-none">
          <%=
            select_tag(
              :switch_to_locale,
              options_for_select(
                available_locales.map do |locale|
                  [I18n.t("spree.i18n.this_file_language", locale: locale, fallback: false, default: locale.to_s), locale, class: 'dropdown-item']
                end.sort,
                selected: I18n.locale
              ),
              class: 'nav-link dropdown-toggle dropdown-lang',  id: 'navbarDropdownLang', href: "#", role: 'button', data: {toggle: 'dropdown'} 
            )
          %>
          <noscript><%= submit_tag t("spree.select") %></noscript>

          <!-- <div class="btn-group-vertical">
            <a class="btn btn-sm btn-outline-primary active" href="#">Eng1</a>
            <a class="btn btn-sm btn-outline-primary" href="#">Eng2</a>
            <a class="btn btn-sm btn-outline-primary" href="#">Eng3</a>
          </div> -->
        </li>
      <% end %>  
    <% end %>
    
    
  <!-- </ul> -->
<!-- </nav> -->
