<%=
  select_tag(
    :switch_to_locale,
    options_for_select(
      available_locales.map do |locale|
        [I18n.t("spree.i18n.this_file_language", locale: locale, fallback: false, default: locale.to_s), locale, class: 'dropdown-item']
      end.sort,
      selected: I18n.locale
    ),
    class: 'nav-link dropdown-toggle dropdown-lang',  id: 'navbarDropdownLang', href: "#", role: 'button', data: {toggle: 'dropdown'} 
  )
%>
<noscript><%= submit_tag t("spree.select") %></noscript>